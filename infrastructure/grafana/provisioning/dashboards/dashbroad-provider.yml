apiVersion: 1
providers:
  - name: 'Velet Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # PATH inside grafana
      path: /etc/grafana/provisioning/dashboards